class Card
  constructor:(@value, @suit) ->
    @currentPile = null
    @isFlipped = no

  getSaveData: ->
    return {
      value: @value
      suit: @suit
    }

  loadData: (data) ->
    @value = data.value
    @suit = data.suit