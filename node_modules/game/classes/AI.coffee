class AI
  decide: (aiCardPile, playerCardPile) ->
    #has the player already busted?
    if playerCardPile.value() > 21
      return MOVE_TYPE.STAY

    #if not, then what's our situation?
    if aiCardPile.value() < 17
      return MOVE_TYPE.HIT
    else
      return MOVE_TYPE.STAY
